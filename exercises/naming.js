/**
 * Identifica qué variables mutan y cuáles no. Organiza y renombra las clases,
 * métodos así como las variables y constantes para que concuerde con el patrón
 * establecido por JavaScript.
 *
 * ¡Por cierto! Esto es un comentario multi línea :-)
 */

var gameversion = 0.1; // Game version

class pokemons {
   constructor(name, type, stats) {
      this.nombre = name;
      this.type = type;
      this.stats = stats;
   }
}

/**
 * El programa debe realizar un cálculo muy simple, va a tomar un valor entero
 * por el usuario como una constante y va a ser elevado para calcular su potencia
 * en base a otro número entero.
 *
 * En caso de realizar la calculadora binaria ten en cuenta el tipo de dato
 * 5 => '101'
 * 2 => '10'
 */

const XX = 5; // Renombra la constante XX
const YY = 2; // Renombra la constante YY

console.log(XX ** YY); // El operador ** es la exponenciación
